# 2026-09 개선 및 개별 되돌리기

각 항목은 별도 Git 커밋으로 관리합니다. 되돌릴 때 해당 소스 커밋을 `git revert`하고 배포 파일을 다시 생성합니다. 원본 엑셀과 고객의 브라우저 저장 자료는 코드 롤백 대상으로 삼지 않습니다.

## 1. 수원 조회 종료

- 수원은 지역 선택 및 정기 갱신에서 제외합니다.
- 원본, 변환 JSON, 갱신 스크립트와 저장된 고객 목록은 삭제하지 않습니다.
- 수원 고객 기록이 있는 경우에만 보관 데이터의 중개사 정보를 읽습니다.
- 되돌리기 대상 커밋 제목: `Archive Suwon browsing without deleting saved work`

## 2. 타입 및 통계 기준 정리

- 타입 선택은 지역·단지·타입으로 구분합니다. 여러 단지 선택 시 타입 메뉴에 단지명을 함께 표시합니다.
- 거래 전체에서는 매매/전세/월세를 분리해 표시합니다. 주차별 표도 거래별 금액을 따로 표시합니다.
- 월세 통계는 환산보증금 기준이며, 다른 거래와 평균내지 않습니다.
- 층 필터를 상단으로 옮겨 요약·브리핑·매물 카드에 공통 적용합니다. 검색 항목도 공유합니다.
- 되돌리기 대상 커밋 제목: `Separate type identities and deal statistics`

## 3. 실거래 원본 보존형 묶음 표시

- 같은 계약일·전용면적·층·금액 자료를 표시용으로 묶으며, 원본 행은 삭제하지 않습니다.
- 최근 15개 조건 묶음과 후보 타입을 표시하고 반복 원본 행을 펼쳐 볼 수 있습니다. 차트도 같은 묶음을 사용합니다.
- 원본에 고유 거래 ID가 없으므로 묶음 수를 실제 거래 건수로 단정하지 않습니다.
- 우리집타입찾기의 유사 면적 자료도 정확히 같은 타입으로 단정하지 않습니다.
- 실거래에 없는 방향·매물 특징·층 구분·예산 조건은 적용하지 않고 집계 범위를 표기합니다.
- 되돌리기 대상 커밋 제목: `Group real transaction candidates without deleting source rows`

## 4. 고객 기록 보존 및 백업

- 담은 매물에 중개사 후보 목록을 함께 저장하여 다음 조사 후에도 보존합니다.
- 이미 사라진 후보를 복원할 수는 없지만, 저장된 선택 중개사와 번호는 계속 표시합니다.
- 물건 리스트도 새로고침 후 유지합니다. 고객별 연락 상태와 내부 메모를 저장하며 고객용 워드에는 내부 메모를 넣지 않습니다.
- 백업은 이 컴퓨터로만 다운로드합니다. 복원은 기존 항목을 덮어쓰지 않고 병합하며, 입력 파일을 검사합니다.
- 기존 저장값이 깨졌을 때 자동 덮어쓰지 않습니다. 변경 전 브라우저 저장값도 복구용으로 남깁니다.
- 되돌리기 대상 커밋 제목: `Preserve customer work with broker snapshots and local backups`
